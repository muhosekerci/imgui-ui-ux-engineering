{
  "E01": "The framework is not established by this prompt, so keep framework choice open. Apply Dear ImGui-specific guidance only after direct API/header evidence or an explicit suitability question.",
  "E02": "Use a stable asset ID; UI state owns selection. Build a filtered/sorted index view without mutating the model, process ImGuiTableSortSpecs, and render visible rows with ImGuiListClipper. Show a no-results state and verify filtering, sorting, selection persistence, and 20,000-row performance.",
  "E03": "Duplicate visible labels create an ID collision. Wrap each row in PushID(item.id) using a stable key, then PopID; the visible label may repeat, but every widget still needs a unique ID. Verify with the ID stack/debug tools.",
  "E04": "This has a branch-dependent stack imbalance and underflows when warning is false. Keep push/pop in the same branch, then run the bundled checker and runtime debug/metrics validation; visual inspection alone is insufficient.",
  "E05": "Choose exactly one FontScaleDpi owner. ConfigDpiScaleFonts can own automatic font scaling on a compatible docking branch; otherwise the host owns it. Reset and apply ScaleAllSizes for size and padding scaling separately, and verify references/version-notes.md plus the vendored imgui.h.",
  "E06": "Never pass null to strcmp, Selectable, TextUnformatted, or formatted %s. Enforce a non-null invariant or route reads through a safe display adapter that returns fallback text such as <Unnamed> or Unknown, then test every nullable field.",
  "E07": "Keep pointer selection separate from programmatic navigation. Store a pending stable ID as a one-shot focus request, consume it after rendering the target, and do not call SetItemDefaultFocus for every selected row every frame. Test command-palette and pointer selection paths.",
  "E08": "Define centralized semantic tokens for surfaces, blue accent, danger, and normal, hover, active, and disabled states. Check normal text at 4.5:1, avoid scattered hard-coded component colors, and document the IMGUI_VERSION_NUM/version assumption.",
  "E09": "First check whether standard widgets can compose the scrubber. If custom drawing remains necessary, define a stable ID and matching hit area; hover, active, focus, and disabled states; drag cancellation; clipping and performance limits; plus keyboard or alternative interaction.",
  "E10": "Guard docking-only API with #if defined(IMGUI_HAS_DOCK). Functional independent windows remain available in the fallback path, and a master build does not guarantee IMGUI_HAS_DOCK; compile both configurations.",
  "F01": "Invalidate and rebuild the derived index view whenever filter, data, or sort-spec input changes, without reordering the source model. Provide a no-results state and a reproduction test that types a filter before touching sort headers.",
  "F02": "Store a stable asset ID in UI state rather than Asset*. Resolve the live object against the live model for each render/action, handle a missing selection safely, and verify both importer refresh and deletion.",
  "F03": "Move EndChild outside the conditional because BeginChild's return value only controls contents; EndChild must always run under the Begin/End contract. Audit other scoped API independently, then run the bundled checker and runtime metrics/debug validation.",
  "F04": "Separate pointer selection from programmatic focus and issue a pending stable ID as a one-shot focus request. Do not reassert focus every frame, which preserves keyboard navigation; test command-palette selection versus pointer selection.",
  "F05": "Reset style before ScaleAllSizes so metric scaling is non-cumulative, and choose exactly one FontScaleDpi owner. Separate font scaling from metric scaling, centralize semantic status tokens, and validate at 100% and 200% DPI."
}
